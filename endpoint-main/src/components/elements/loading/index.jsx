import React from 'react'
import { CircularProgress, Box } from '@mui/material'

export default function index(props) {
  const { } = props

  return (
    <Box sx={{ m: 1 }} className="flex jcc">
      <CircularProgress />
    </Box>
  )
}
